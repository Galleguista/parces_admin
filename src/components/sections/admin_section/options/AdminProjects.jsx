import React from 'react';
import { Typography, Box } from '@mui/material';

const AdminProjects = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h5">Gestión de Proyectos</Typography>
      {/* Más contenido y lógica aquí */}
    </Box>
  );
};

export default AdminProjects;
